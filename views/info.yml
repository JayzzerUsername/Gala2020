path: /info
template: info
context:
  static:
    pagetitle: Informations
    meta:
      description: Informations sur les tarifs et les navettes.
      keywords:
        - informations
        - tarifs
        - navettes
  dynamic:
    faq: session.query(FAQ).order_by(FAQ.order).all()
