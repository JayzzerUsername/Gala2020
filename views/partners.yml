path: /partners
template: partners
context:
  static:
    pagetitle: Partenaires
    meta:
      description: Descriptions des partenaires qui ont rendu possible cette événement.
      keywords:
        - description
        - partenaires
  dynamic:
    partners: session.query(Partner).order_by(Partner.order).all()
