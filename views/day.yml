path: /day
template: day
context:
  static:
    pagetitle: Journée
    meta:
      description: Description du déroulement de la journée.
      keywords:
        - description
        - journée
        - déroulement
  dynamic:
    timeline: session.query(Timeline).order_by(Timeline.order).all()
